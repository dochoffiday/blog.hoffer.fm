@model BC.Models.BC_Page

@{
    ViewBag.Title = Model.Title;
}

<div class="mtop row">
    <div class="onecol">
        &nbsp;
    </div>
    <div class="tencol">
        <h1>@Model.Title.ToUpper()</h1>
        @Html.Raw(Model.Body)
    </div>
    <div class="onecol last">
        &nbsp;
    </div>
</div>

